{
  "name": "neomux",
  "version": "0.1.0",
  "description": "Cursor-style tmux cockpit for Neovim with an EdgeOne-backed Rust coding agent.",
  "type": "module",
  "files": [
    "bin",
    "src",
    "edgeone",
    "scripts",
    "docs",
    "Cargo.toml",
    "Cargo.lock",
    ".env.example",
    "CHANGELOG.md",
    "README.md"
  ],
  "bin": {
    "neomux": "./bin/neomux.js"
  },
  "scripts": {
    "install": "cargo build --release",
    "start": "cargo run --",
    "agent": "cargo run -- agent",
    "check": "cargo fmt --check && cargo clippy -- -D warnings && cargo build --release && node --check ./bin/neomux.js && node --check ./scripts/edgeone-build.js && node --check ./scripts/package.js && node --check ./edgeone/cloud-functions/api/chat.js && node --check ./edgeone/cloud-functions/api/usage.js && node --check ./edgeone/cloud-functions/api/models.js",
    "build": "cargo build --release",
    "package": "node ./scripts/package.js",
    "edgeone:build": "node ./scripts/edgeone-build.js",
    "edgeone:dev": "edgeone makers dev",
    "edgeone:deploy": "edgeone makers deploy ./dist"
  },
  "engines": {
    "node": ">=22"
  }
}
