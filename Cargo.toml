[package]
name = "neomux"
version = "0.1.0"
edition = "2024"
description = "Cursor-style tmux cockpit for Neovim with an EdgeOne-backed coding agent."
license = "MIT"

[profile.release]
strip = true
lto = true
codegen-units = 1
