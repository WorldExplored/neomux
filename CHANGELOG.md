# Changelog

## 0.1.0

- Rust CLI binary.
- tmux launcher with Rose Pine styling.
- Neovim left pane and EdgeOne-backed agent right pane.
- `doctor`, `models`, `config`, and `version --json`.
- EdgeOne deploy package build via `npm run edgeone:build`.
